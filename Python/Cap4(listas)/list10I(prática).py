import random

messages = ['It is certain', 'it is decidely so', 'yes definitely', 'reply hazy try again', 'ask again later', 
'Concentrate and ask again', 'my reply is no', 'outlook not so good', 'very doubtful']

print(messages[random.randint(0, len(messages) )]) #printar messages, de forma randomica, 
#de 0 a len(messages) menos 1.

# +\ < indica que a instrução continuará na próxima linha